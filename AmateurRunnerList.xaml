<Window x:Class="PrestonMarathonApp.AmateurRunnerList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="MainWindow" Height="780" Width="1280" Background="Transparent" WindowStartupLocation="CenterScreen" AllowsTransparency="True" WindowStyle="None">

    <Border CornerRadius="25" Padding="20" Background="#f7f6f4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <!-- Left section -->
            <Border CornerRadius="25" Background="#F6E063" Padding="15">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Name="db"></TextBlock>
                    <TextBlock TextAlignment="Center" FontSize="28" FontWeight="SemiBold">Preston Marathon-2022</TextBlock>
                    <Image Source="/images/1.png" Margin="0 50 0 10"></Image>
                </StackPanel>
            </Border>


            <!--List section-->
            <Border Grid.Column="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="100"></RowDefinition>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Vertical">
                        <TextBlock FontSize="35" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center">Amateur Runner List</TextBlock>
                       

                    </StackPanel>

                    <DataGrid  x:Name="AmaterListGrid" HeadersVisibility="Column" Margin="10,75,38,94" AutoGenerateColumns="false" CanUserAddRows="False"  Background="White" Grid.RowSpan="2" BorderBrush="#FF528EC8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="100" Header="RunnerNo" Binding="{Binding runnerNo}" ></DataGridTextColumn>
                            <DataGridTextColumn Width="100" Header="First Name" Binding="{Binding particpiantFirstName}"></DataGridTextColumn>
                            <DataGridTextColumn Width="100" Header="Last Name" Binding="{Binding particpiantLastName}"></DataGridTextColumn>
                            <DataGridTextColumn Width="100" Header="Status" Binding="{Binding participationStatus}"></DataGridTextColumn>
                            <DataGridTextColumn Width="120" Header="Costume" Binding="{Binding runnerCostume}"></DataGridTextColumn>
                            <DataGridTemplateColumn Width="*" IsReadOnly="True"  Header="More operations">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Name="add_sponsor" Click="AddSponsor" Tag="{Binding participantId}" Style="{StaticResource gridSponsorButton}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">Add Sponsor</TextBlock>
                                            </Button>
                                            <Button Name="add_costume" Tag="{Binding participantId}" Click="AddCostume" Style="{StaticResource gridCostumeButton}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">Add Costume</TextBlock>
                                            </Button>
                                            <Button Name="ViewDetails" Click="ViewDetails" Tag="{Binding participantId}" Style="{StaticResource gridStatusButton}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                                <fa:FontAwesome Icon="Eye" ToolTip="Update runner status"/>
                                            </Button>
                                            <Button Name="update_status" Click="update_particpant_status" Tag="{Binding participantId}" Style="{StaticResource gridStatusButton}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                                <fa:FontAwesome Icon="FlagCheckered" ToolTip="Update runner status"/>
                                            </Button>
                                        </StackPanel>
                                        
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>

            </Border>

        </Grid>

    </Border>

</Window>
